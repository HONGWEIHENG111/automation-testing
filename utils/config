import os

# ==========================================
# 0. 动态路径解析 (Dynamic Path Resolution)
# ==========================================
# 当前 config.py 位于 project_root/utils/ 目录下
_CURRENT_DIR = os.path.dirname(os.path.abspath(__file__))
# 向上回溯一层，获取工程根目录 project_root/
PROJECT_ROOT = os.path.dirname(_CURRENT_DIR)
# ==========================================
# 1. 账号与凭证信息 (Credentials & API Keys)
# ==========================================
# 网站登录账号
USERNAME = ""
# 网站登录密码
PASSWORD = ""

# --- 多网页并行环境独立账号密码 (供 main_multiweb.py 调用) ---
USERNAME_1 = ""
PASSWORD_1 = ""

USERNAME_2 = ""  # 若环境2账号不同，可在此处单独修改
PASSWORD_2 = ""  # 若环境2密码不同，可在此处单独修改
# DeepSeek API Key (统一管理，供评价器和总结生成器调用)
DEEPSEEK_API_KEY = ""

# ==========================================
# 2. 网址与接口 (URLs)
# ==========================================
# 测试目标网站主页
HOME_URL = "https://192.168.1.104/"
#https://192.168.1.104/
#https://customs-demo.poffices.ai/
# DeepSeek API 请求基准地址
DEEPSEEK_BASE_URL = "https://api.deepseek.com"

# --- 多网页并行环境独立网址 ---
URL_1 = "https://192.168.1.104/"
ENV_NAME_1 = "customs"

URL_2 = "https://poffices.ai/"
ENV_NAME_2 = "poffices"

# ==========================================
# 3. 文件路径 (Paths)
# ==========================================
# Excel 文件名（直接放在工程根目录下）
EXCEL_FILENAME = ""
# 动态拼接成绝对路径，无论在谁的电脑上跑，或者工程文件夹搬到哪里，都能自动识别
INPUT_EXCEL_PATH = os.path.join(PROJECT_ROOT, EXCEL_FILENAME)
#==========================================
# 4. 性能与并发配置 (Performance & Concurrency)
# ==========================================
# 同时开启的浏览器/网页最大数量 (根据电脑性能调整，推荐 3-5)
MAX_WORKERS = 3
# ==========================================
# 5. 总结报告路径 (Summary Report Paths)
# ==========================================
# 根 Summary 目录
SUMMARY_ROOT_DIR = os.path.join(PROJECT_ROOT, "Summaries")

# 针对 multiweb 的子目录
SUMMARY_ENV_1_DIR = os.path.join(SUMMARY_ROOT_DIR, f"Summaries_{ENV_NAME_1}")
SUMMARY_ENV_2_DIR = os.path.join(SUMMARY_ROOT_DIR, f"Summaries_{ENV_NAME_2}")
